# what is flexbox?
flexbox is a CSS layout system usd to allign and destribute space between items inside a container 

# why css hard?
mainly because of many rules and defaults
confusion in giving spacing values
small changes affecting many elements

# one thing clicked:
